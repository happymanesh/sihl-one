# Local dev points at STAGING, which holds synthetic data only.
# Never point this at the production API - the CUG's real data is there.
NEXT_PUBLIC_API_BASE_URL=https://api-staging-4ba5.up.railway.app/api/v1
API_INTERNAL_BASE_URL=https://api-staging-4ba5.up.railway.app/api/v1

# Dictation stays off until the Bhashini key is issued. Set to true to enable -
# a config change on the running service, no rebuild needed.
VOICE_INPUT_ENABLED=false
